"""ML model wrappers and (optional) training artifacts.

The XGBoost DDI severity model is optional — when not present, the tool falls
back to a transparent heuristic defined in argus.tools.check_interactions.

Train via: `python scripts/train_ddi_model.py`
"""
