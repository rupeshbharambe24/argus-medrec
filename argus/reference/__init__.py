"""Reference knowledge base — seed data + SQLite builder.

Run `python -m argus.reference.build_kb` to (re)populate argus/reference/reference.sqlite
from the CSV files in argus/reference/data/.

The generated DB is gitignored; regenerate anytime.
"""
